# organize-receipt

Receipt and expense tracking apps.

## Layout

- **`g-doc/`** – Streamlit app using **Google Sheets** and **Google Drive** (config via `.env`). Run from that folder: `cd g-doc && uv sync && uv run streamlit run app.py`.
- **Supabase** – Planned; new app will live at repo root or in a separate folder.
